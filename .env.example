# Example environment values for local scaffold tooling
# Replace these with real values only in local environment files that are not committed.

PROJECT_NAME=everything-game-dev-code
DEFAULT_PROFILE=preproduction-only
DEFAULT_ENGINE=
